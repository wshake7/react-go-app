import type { ThemeConfig } from 'antd'

export const theme = {
  token: {
    colorPrimary: '#4a79d8',
  },
  cssVar: true
} as ThemeConfig

export const config = {
  // 主题配置
  theme,
  // 主页路径
  homePath: '',
}
